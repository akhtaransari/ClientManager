package com.sunbase.clientmanager.dto;

/**
 * A record for representing user credentials.
 *
 * This record holds a single field, `password`, which is used for
 * authentication purposes.
 */
public record Password(String password) {
}
